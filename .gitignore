# OS files
.DS_Store
Thumbs.db

# Editor files
.vscode/
.idea/
*.swp
*.swo
*~

# Environment / credentials
.env
.env.local
.env.production

# The browser-safe Supabase anon key is versioned so Netlify can deploy auth.
# Keep service-role keys and other secrets in ignored environment files.

# Accidental npm artifacts
node_modules/
package-lock.json
